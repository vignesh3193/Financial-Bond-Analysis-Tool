README-GROUPHW2

Step 1: make

Step 2:

A) RUn everything from the same terminal

1. Run-> ./test_server.sh -> client commands can be entered in the same terminal(screenshot attached)
2. ENTER-> RUN/EXIT

B) Alternate Way(Run client and server on separate windows)

1. Run serverL ./testdriver
2. Run Client time script: ./test.sh followed by RUN/EXIT